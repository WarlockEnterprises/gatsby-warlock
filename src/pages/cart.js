import * as React from "react"
import Layout from "../components/layout"

const CartPage = () => (
  <Layout title={"Prints"}>
    <h1 className="text-center p-5 mt-5">Cart</h1>
  </Layout>
)

export default CartPage
